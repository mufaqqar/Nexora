<a class="brand" href="{{ routes.root_url }}" aria-label="{{ shop.name }}">
  <span class="brand-mark" aria-hidden="true">N</span>
  <span class="brand-name">{{ settings.brand_name | default: shop.name | escape }}</span>
</a>
