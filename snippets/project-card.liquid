{% assign current = product.metafields.project.total_purchases.value | default: 0 %}
{% assign target = product.metafields.project.target_amount.value | default: 1 %}
{% assign funded = current | times: 100 | divided_by: target %}
{% assign customers = product.metafields.project.customers.value | default: 0 %}
{% assign category = product.metafields.project.category.value | default: 'Technologie' %}

<article class="project-card">
  <a class="project-card__media" href="{{ product.url }}">
    {% render 'project-visual', image: product.featured_image, loading: 'lazy' %}
    
  </a>

  <div class="project-card__body">
    <div class="project-card__top">
        <h3><a href="{{ product.url }}">{{ product.title }}</a></h3>
       <span class="project-card__category">{{ category }}</span>
    </div>

    {% if product.description != blank %}
      <p class="project-card__description">{{ product.description | strip_html | truncate: 105 }}</p>
    {% endif %}

    <div class="project-card__stats">
      <div class="project__stat project__stat--highlight">
        <strong>{{ funded }}%</strong>
        <span>Financé</span>
      </div>
      <div class="project__stat">
        <strong>{{ current | money_without_trailing_zeros }}</strong>
        <span>Total achats / {{ target | money_without_trailing_zeros }}</span>
      </div>
      <div class="project__stat">
        <strong>{{ customers }}</strong>
        <span>Clients</span>
      </div>
    </div>

    <div class="progress">
      <span style="width: {{ funded }}%"></span>
    </div>
 </div>
    <a class="project__button--secondary" href="{{ product.url }}?view=view-project">
      Voir le projet
    </a>
 
</article>
